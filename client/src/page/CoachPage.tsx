import { Sparkles } from "lucide-react";
import { PageShell } from "./PageShell";

export function CoachPage() {
  return <PageShell icon={Sparkles} title="AI Coach" description="Rocco's AI coaching experience can render here while sharing the authenticated FitPilot layout." />;
}
