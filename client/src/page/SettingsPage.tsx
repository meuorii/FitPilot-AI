import { Settings } from "lucide-react";
import { PageShell } from "./PageShell";

export function SettingsPage() {
  return <PageShell icon={Settings} title="Settings" description="Profile, preferences, goals, and account settings can continue in this authenticated route." />;
}
